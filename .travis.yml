osx_image: xcode8.2
language: objective-c
sudo: false
before_install:
    - rvm use system
    - npm cache clean -f
    - npm install -g n
    - n stable
    - node --version
    - xcodebuild -version
install:
    - npm install
script:
    - npm test
